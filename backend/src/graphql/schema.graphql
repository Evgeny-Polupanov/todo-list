type User {
    _id: ID!
    email: String!
    name: String!
    password: String!
    todos: [ID!]
}

input UserInput {
    email: String!
    name: String!
    password: String!
}

type Todo {
    _id: ID!
    content: String!
    isDone: Boolean!
    creator: ID!
}

type UserData {
    token: String!
    _id: ID!
}

type Query {
    getTodos: [Todo!]
}

type Mutation {
    signup(userInput: UserInput): ID!
}

schema {
    query: Query
    mutation: Mutation
}
